# audio-analytics-pipeline-aws
End-to-end audio analytics pipeline built using AWS S3, Glue, and Athena. Performs ETL on streaming data, creates curated tables, and applies logistic regression in Python to predict listener completion. Includes ROC, PR, and confusion matrix evaluations.
